if TARGET_BIO

config SYS_BOARD
	default "bio"

config SYS_VENDOR
	default "boundary"

config SYS_CONFIG_NAME
	default "bio"

config DDR_RANK_BITS
	int "ddr rank bits"
	default 1

source "board/boundary/common/Kconfig"

endif
