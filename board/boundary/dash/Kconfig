if TARGET_DASH

config SYS_CPU
	default "armv7"

config SYS_BOARD
	default "dash"

config SYS_VENDOR
	default "boundary"

config SYS_SOC
	default "mx6"

config SYS_CONFIG_NAME
	default "dash"

source "board/boundary/common/Kconfig"

endif
