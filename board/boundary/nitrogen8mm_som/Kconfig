if TARGET_NITROGEN8MM_SOM

config SYS_BOARD
	default "nitrogen8mm_som"

config SYS_VENDOR
	default "boundary"

config SYS_CONFIG_NAME
	default "nitrogen8mm_som"

config DDR_RANK_BITS
	int "ddr rank bits"
	default 1

source "board/boundary/common/Kconfig"

endif
