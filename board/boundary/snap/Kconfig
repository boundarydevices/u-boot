if TARGET_SNAP

config SYS_CPU
	default "armv7"

config SYS_BOARD
	default "snap"

config SYS_VENDOR
	default "boundary"

config SYS_SOC
	default "mx6"

config SYS_CONFIG_NAME
	default "snap"

source "board/boundary/common/Kconfig"

endif
