if TARGET_DWE

config SYS_BOARD
	default "dwe"

config SYS_VENDOR
	default "boundary"

config SYS_CONFIG_NAME
	default "dwe"

config DDR_RANK_BITS
	int "ddr rank bits"
	default 1

source "board/boundary/common/Kconfig"

endif
