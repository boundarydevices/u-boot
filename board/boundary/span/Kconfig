if TARGET_SPAN

config SYS_BOARD
	default "span"

config SYS_VENDOR
	default "boundary"

config SYS_CONFIG_NAME
	default "span"

config DDR_RANK_BITS
	int "ddr rank bits"
	default 1

source "board/boundary/common/Kconfig"

endif
