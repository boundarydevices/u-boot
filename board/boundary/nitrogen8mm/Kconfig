if TARGET_NITROGEN8MM

config SYS_BOARD
	default "nitrogen8mm"

config SYS_VENDOR
	default "boundary"

config SYS_CONFIG_NAME
	default "nitrogen8mm"

config DDR_RANK_BITS
	int "ddr rank bits"
	default 1

source "board/boundary/common/Kconfig"

endif
