if TARGET_NITROGEN6X

config SYS_BOARD
	default "nitrogen6x"

config SYS_VENDOR
	default "boundary"

config SYS_CONFIG_NAME
	default "nitrogen6x"

config ENV_WLMAC
	bool
	default	y

source "board/boundary/common/Kconfig"

endif
